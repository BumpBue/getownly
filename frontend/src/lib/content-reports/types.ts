/**
 * Re-exported from @getownly/shared rather than declared here a second time —
 * see frontend/src/lib/catalog/types.ts for why.
 */

import {
  CONTENT_REPORT_STATUSES,
  CONTENT_REPORT_TARGET_TYPES,
  type ContentReport,
  type ContentReportStatus,
  type ContentReportTargetType,
  type PaginatedContentReports,
} from '@getownly/shared';

export { CONTENT_REPORT_STATUSES, CONTENT_REPORT_TARGET_TYPES };
export type {
  ContentReport,
  ContentReportStatus,
  ContentReportTargetType,
  PaginatedContentReports,
};
