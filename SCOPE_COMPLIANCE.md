# SCOPE_COMPLIANCE.md — ตารางเทียบระบบกับเอกสาร ทก.01

**โครงงาน:** แพลตฟอร์มจำหน่ายและจัดการคอร์สเรียนออนไลน์สำหรับผู้สอนอิสระ
(Online Course Marketplace and Management Platform)

**วันที่จัดทำ:** 8 กันยายน 2569
**เอกสารอ้างอิง:** ทก.01 ฉบับผ่านความเห็นชอบของคณะกรรมการ

เอกสารฉบับนี้เทียบข้อกำหนดในเอกสาร ทก.01 กับสิ่งที่ระบบทำได้จริง ณ ปัจจุบัน ทีละข้อ
พร้อมระบุไฟล์หรือ endpoint ที่รองรับข้อนั้น เพื่อให้ตรวจสอบย้อนกลับไปยังโค้ดได้

**สรุปโดยย่อ:** ข้อกำหนดทั้งหมด **37 ข้อ** (A1–A10, B1–B11, C1–C7, D1–D9) **ทำครบทุกข้อ**
ระบบมี endpoint 82 เส้นทาง · migration 12 ใบ · ชุดทดสอบอัตโนมัติ 422 ข้อ ผ่านทั้งหมด

---

## 1. ความหมายของสถานะ

| สถานะ | ความหมาย |
|---|---|
| ✅ ครบ | ทำได้ตามที่เอกสารกำหนด และมีชุดทดสอบอัตโนมัติครอบคลุม |
| ✅ ครบ (ไม่มีเทสต์อัตโนมัติ) | ทำได้ตามที่กำหนด แต่พิสูจน์ด้วยการตรวจด้วยตา ไม่ใช่ชุดทดสอบ |

---

## 2. ผู้สอน (Instructor) — ข้อ A1–A10

| ข้อ | ข้อกำหนดใน ทก.01 | สถานะ | รองรับด้วย |
|---|---|---|---|
| A1 | สมัครสมาชิกในฐานะผู้สอน และเข้าสู่ระบบด้วยชื่อผู้ใช้/รหัสผ่าน | ✅ ครบ | `POST /auth/register` (เลือกบทบาทได้เฉพาะ STUDENT/INSTRUCTOR) · `POST /auth/login` (รับได้ทั้งอีเมลและชื่อผู้ใช้) · `backend/src/modules/auth/` |
| A2 | จัดการโปรไฟล์: ชื่อที่แสดง รูปภาพ ประวัติ ความเชี่ยวชาญ | ✅ ครบ | `PATCH /users/me` · `PATCH /users/me/avatar` · หน้า `/profile` |
| A3 | สร้างรายวิชาได้มากกว่าหนึ่งรายวิชา (ชื่อ คำอธิบาย ภาพปก หมวดหมู่) | ✅ ครบ | `POST /courses` · `PATCH /courses/:id` · หน้า `/instructor/courses/new` และ `/instructor/courses/[id]` |
| A4 | กำหนดราคาได้อิสระ **ไม่เกิน 10,000 บาท** หรือกำหนดเป็นรายวิชาฟรี | ✅ ครบ | บังคับ **3 ชั้น** — ดูตารางข้อ 6 |
| A5 | จัดโครงสร้างเป็นบทเรียน อัปโหลดวิดีโอ แนบเอกสาร (PDF / ไฟล์นำเสนอ) | ✅ ครบ | `POST /courses/:id/lessons` · `POST /lessons/:id/materials` · รับ PDF DOCX PPTX XLSX ZIP · `backend/src/modules/uploads/upload-rules.ts` |
| A6 | วิดีโอไม่เกิน **150 MB ต่อคลิป** และพื้นที่จัดเก็บไม่เกิน **1 GB ต่อรายวิชา** | ✅ ครบ | นับยอดสะสมจริงผ่าน `Course.storageUsedBytes` ตรวจ 2 จุด (ตอนขอ presign และตอนไฟล์ลงจริง) · `CourseAccessService.assertStorageAvailable()` |
| A7 | สร้างแบบทดสอบท้ายบทเรียนแบบปรนัย แยกอิสระต่อบทเรียน กำหนดคำถาม ตัวเลือก เฉลย และเกณฑ์ผ่านเอง (**60–100%**) | ✅ ครบ | `POST /lessons/:id/quiz` · `PATCH /quizzes/:id` · `DELETE /quizzes/:id` · หน้าจอแท็บ "แบบทดสอบ" ใน `/instructor/courses/[id]` · หนึ่งชุดต่อหนึ่งบทเรียน (unique index) |
| A8 | ตอบคำถามผู้เรียนในกระดานถาม-ตอบของรายวิชา | ✅ ครบ | `POST /qna/:threadId/replies` · กล่องคำถามรวมที่ `/instructor/qna` |
| A9 | เรียกดูรายชื่อผู้เรียน ความก้าวหน้า คะแนนแบบทดสอบ และสรุปยอดขายของแต่ละรายวิชา | ✅ ครบ | `GET /instructor/courses/:id/students` · แท็บ "ผู้เรียน" ในหน้าแก้ไขคอร์ส · ยอดขายรายคอร์สที่ `GET /instructor/reports/overview` |
| A10 | ดูยอดรายได้คงเหลือหลังหักส่วนแบ่ง พร้อม**รายละเอียดการคำนวณรายธุรกรรม** | ✅ ครบ | `GET /instructor/reports/transactions` แสดง 4 ค่าต่อรายการ: ราคาขาย / อัตราส่วนแบ่ง ณ เวลานั้น / จำนวนที่ถูกหัก / ยอดสุทธิ · ตารางในหน้า `/instructor/reports` |

### หมายเหตุทางวิชาการของข้อ A10

อัตราส่วนแบ่งที่แสดงเป็นค่าที่ **บันทึกไว้ ณ เวลาที่เกิดการขาย** (`Enrollment.commissionRateSnapshot`)
ไม่ใช่อัตราปัจจุบันของผู้สอน ถ้าผู้ดูแลระบบแก้อัตราส่วนแบ่งภายหลัง รายงานย้อนหลังจะไม่ขยับตาม
ส่วนจำนวนเงินทั้งสามค่าอ่านจากตาราง `LedgerEntry` โดยตรง ไม่ได้คำนวณใหม่ตอนเปิดรายงาน
จึงตรงกับงบทดลองเสมอ

---

## 3. ผู้เรียน (Student) — ข้อ B1–B11

| ข้อ | ข้อกำหนดใน ทก.01 | สถานะ | รองรับด้วย |
|---|---|---|---|
| B1 | สมัครสมาชิกด้วยชื่อผู้ใช้/รหัสผ่าน และขอตั้งรหัสผ่านใหม่ผ่านอีเมล | ✅ ครบ | `POST /auth/forgot-password` · `POST /auth/reset-password` · โทเคนเก็บแบบ hash ใช้ได้ครั้งเดียว และเพิกถอน session ทั้งหมดหลังรีเซ็ต |
| B2 | หน้าหลักแสดงรายวิชาทั้งหมดจากผู้สอนทุกรายในระบบ | ✅ ครบ | `GET /courses` (สาธารณะ) · หน้า `/courses` |
| B3 | ค้นหา/กรองตาม ชื่อ หมวดหมู่ **ผู้สอน** และช่วงราคา พร้อมหน้ารายละเอียดก่อนตัดสินใจ | ✅ ครบ | ตัวกรองทั้งสี่ทำงานร่วมกันได้พร้อมกัน · `GET /courses/instructors` ให้รายชื่อผู้สอนที่มีคอร์สเผยแพร่แล้วเท่านั้น · หน้า `/courses/[id]` |
| B4 | รายวิชาฟรีสมัครเข้าเรียนได้ทันที | ✅ ครบ | `POST /courses/:id/purchase` สร้างสิทธิ์เข้าเรียนโดยไม่เขียนรายการบัญชี เพราะไม่มีเงินเคลื่อนไหว |
| B5 | เติมเงินโดยโอนเงินและแนบหลักฐาน แล้วชำระค่ารายวิชาจากยอดคงเหลือ | ✅ ครบ | `POST /topups/quote` (QR PromptPay) · `POST /topups` (แนบสลิป) · ซื้อคอร์สตัดจากกระเป๋าเท่านั้น ไม่มีการจ่ายตรงรายครั้ง |
| B6 | จัดการโปรไฟล์ และเมนู "รายวิชาของฉัน" แสดงทั้งที่ซื้อและที่ลงทะเบียนฟรี | ✅ ครบ | `GET /enrollments/mine` · หน้า `/my-courses` |
| B7 | เข้าเรียนตามลำดับที่ต้องการอย่างอิสระ (**Soft Lock**) | ✅ ครบ | ไม่มีการตรวจลำดับบทเรียนหรือคะแนนสอบก่อนเข้าบทถัดไปที่ใดในระบบ ทั้ง `LearnService` และหน้าจอ |
| B8 | สิทธิ์เข้าถึงรายวิชาที่ซื้อแล้ว **ไม่มีวันหมดอายุ** | ✅ ครบ | ตาราง `Enrollment` ไม่มีคอลัมน์วันหมดอายุหรือการต่ออายุใดๆ |
| B9 | ตั้งคำถามในกระดานถาม-ตอบของรายวิชาที่ลงทะเบียนไว้ | ✅ ครบ | `POST /courses/:id/qna` · เข้าถึงได้เฉพาะผู้ลงทะเบียน |
| B10 | แจ้งเนื้อหาไม่เหมาะสม (ทั้งรายวิชาและกระทู้) พร้อมระบุเหตุผล | ✅ ครบ | `POST /content-reports` · บังคับกรอกเหตุผลอย่างน้อย 10 ตัวอักษร |
| B11 | ติดตามความก้าวหน้าและผลคะแนนแบบทดสอบของตนเอง | ✅ ครบ | `GET /learn/:courseId` · `GET /quizzes/:id/attempts/mine` · หน้าเฉลยพร้อมประวัติทุกครั้งที่ทำ |

---

## 4. ผู้ดูแลระบบ (Admin) — ข้อ C1–C7

| ข้อ | ข้อกำหนดใน ทก.01 | สถานะ | รองรับด้วย |
|---|---|---|---|
| C1 | จัดการบัญชีผู้ใช้ทั้งหมด รวมถึงระงับ/เปิดใช้งานบัญชี | ✅ ครบ | `GET /admin/users` · `PATCH /admin/users/:id/status` · หน้า `/admin/users` |
| C2 | ตรวจสอบและอนุมัติรายการเติมเงินที่แนบหลักฐานโอน | ✅ ครบ | `GET /admin/topups` · `PATCH /admin/topups/:id/approve` และ `/reject` · หน้า `/admin/topups` |
| C3 | กำหนดอัตราส่วนแบ่งรายได้ของผู้สอน**แต่ละราย** | ✅ ครบ | `PATCH /admin/users/:id/commission` · เก็บที่ `User.commissionRate` เป็นรายคน ไม่ใช่ค่าเดียวทั้งระบบ |
| C4 | ตรวจสอบและอนุมัติรายวิชาก่อนเผยแพร่ (draft → pending → approved/rejected) | ✅ ครบ | `GET /admin/courses/pending` · `PATCH /admin/courses/:id/approve` และ `/reject` · enum `CourseStatus` |
| C5 | ตรวจรายการแจ้งเนื้อหาไม่เหมาะสม และระงับการมองเห็นรายวิชาชั่วคราว | ✅ ครบ | `GET /admin/content-reports` · `PATCH /admin/content-reports/:id/review` (ระงับได้) และ `/restore-course` (คืนสถานะ) |
| C6 | จัดการหมวดหมู่รายวิชา | ✅ ครบ | `GET/POST/PATCH/DELETE /admin/categories` · หน้า `/admin/categories` |
| C7 | รายงานสรุปผลการดำเนินงานของแพลตฟอร์ม | ✅ ครบ | `/admin/reports` ครบ 5 ชนิดตามข้อ D7 |

---

## 5. การทำงานของระบบ — ข้อ D1–D9

| ข้อ | ข้อกำหนดใน ทก.01 | สถานะ | รองรับด้วย |
|---|---|---|---|
| D1 | RBAC ตรวจสิทธิ์ทุก request ก่อนเข้าถึงข้อมูล | ✅ ครบ | `JwtAuthGuard` + `RolesGuard` ทำงานเป็น global guard ทุกเส้นทาง — ต้องล็อกอินเป็นค่าเริ่มต้น เส้นทางสาธารณะต้องประกาศ `@Public()` อย่างจงใจ · ชั้นที่สองตรวจความเป็นเจ้าของผ่าน `CourseAccessService` |
| D2 | Wallet ledger บันทึกทุกรายการเคลื่อนไหวของทั้งผู้เรียนและผู้สอน ยอดคงเหลือตรวจย้อนกลับได้ | ✅ ครบ | ระบบบัญชีคู่ `LedgerTransaction` + `LedgerEntry` · ยอดใน `Account.balance` เป็นเพียงยอดสรุปที่เขียนพร้อมกันในธุรกรรมเดียวกัน · `GET /wallet` เปิดให้ทุกบทบาท |
| D3 | Mock Payment: แนบหลักฐาน + ผู้ดูแลยืนยัน โดยมีเป้าหมายอนุมัติภายใน 24 ชั่วโมง | ✅ ครบ | ไม่มีการเชื่อมต่อ payment gateway ใดๆ · แจ้งเป้าหมาย 24 ชม. บนหน้าเติมเงินก่อนโอน · คิวผู้ดูแลมีคอลัมน์ "รอมาแล้ว" และตัวนับ "เกิน 24 ชม. N รายการ" |
| D4 | แบบทดสอบปรนัยเท่านั้น ตรวจให้คะแนนอัตโนมัติ ห้ามมีข้อสอบอัตนัยและคลังข้อสอบกลาง | ✅ ครบ | ตาราง `QuizQuestion` มีได้เฉพาะตัวเลือก ไม่มีชนิดคำถามแบบเขียนตอบ · ตรวจคะแนนที่เซิร์ฟเวอร์ เฉลยไม่เคยออกไปขณะทำข้อสอบ · ไม่มีตารางคลังข้อสอบกลางในฐานข้อมูล |
| D5 | กระดานถาม-ตอบแยกตามรายวิชา เข้าถึงได้เฉพาะผู้ลงทะเบียนและผู้สอนของวิชานั้น | ✅ ครบ | `CourseAccessService.resolveQnaAccess()` เป็นจุดตัดสินสิทธิ์จุดเดียว |
| D6 | ระบบแจ้งและจัดการเนื้อหาไม่เหมาะสม | ✅ ครบ | โมดูล `content-reports` ครบวงจรตั้งแต่แจ้ง ตรวจ ระงับ ถึงคืนสถานะ |
| D7 | รายงานครบ 5 ชนิด | ✅ ครบ | ยอดขายรวม + แยกตามผู้สอน · ส่วนแบ่งรายได้แพลตฟอร์ม · รายวิชายอดนิยม · จำนวนสมาชิกใหม่ · ยอดค้างจ่ายผู้สอน — ทุกตัวอ่านจาก `LedgerEntry` และคิดตามเวลาไทย (Asia/Bangkok) |
| D8 | Responsive ตั้งแต่มือถือ แท็บเล็ต ถึงเดสก์ท็อป | ✅ ครบ | ตรวจจริงด้วยเบราว์เซอร์อัตโนมัติที่ความกว้าง 375 / 768 / 1440 px ครอบทุกหน้าหลักของทั้งสามบทบาท — ดูข้อ 8 |
| D9 | **ACID Transaction ตอนซื้อรายวิชา** ทำครบ 4 ขั้นในธุรกรรมเดียว ถ้าพลาดต้อง rollback ทั้งหมด | ✅ ครบ | `WalletService.purchaseCourse()` — ดูข้อ 7 |

---

## 6. ตัวเลขที่เอกสารกำหนด เทียบกับที่ระบบบังคับจริง

ค่าทั้งหมดประกาศไว้ที่ไฟล์เดียวคือ **`packages/shared/src/limits.ts`** ทั้งเว็บและ API อ่านจากที่นั่น
จึงไม่มีทางที่สองฝั่งจะบังคับคนละตัวเลข

| ข้อกำหนด | ค่าที่ ทก.01 กำหนด | ค่าที่ระบบบังคับ | บังคับกี่ชั้น |
|---|---|---|---|
| ราคาสูงสุดต่อรายวิชา (A4) | 10,000 บาท | 10,000 บาท | **3 ชั้น** — เว็บ (zod) · API (DTO) · **ฐานข้อมูล** (`CHECK Course_price_within_scope`) |
| รายวิชาฟรี (A4) | ต้องกำหนดได้ | ราคา 0 บาท ใช้งานได้ ไม่เขียนรายการบัญชี | 3 ชั้น (ขอบล่างของ CHECK เดียวกัน) |
| ขนาดวิดีโอต่อคลิป (A6) | 150 MB | 150 MB | **2 ชั้น** — เว็บตรวจก่อนอัปโหลด · API ตรวจทั้งตอนขอ presign และตอนไฟล์ลงจริง |
| พื้นที่จัดเก็บต่อรายวิชา (A6) | 1 GB | 1 GB (1,073,741,824 ไบต์) | **2 ชั้น** — API ตรวจ 2 จุดเทียบกับยอดสะสมจริง · เว็บแสดงแถบพื้นที่คงเหลือ |
| เกณฑ์คะแนนผ่าน (A7) | 60–100% | 60–100% | **3 ชั้น** — เว็บ · API (DTO) · **ฐานข้อมูล** (`CHECK Quiz_passScore_within_scope`) |
| เป้าหมายอนุมัติเติมเงิน (D3) | 24 ชั่วโมง | 24 ชั่วโมง | แสดงต่อผู้เรียนและนับให้ผู้ดูแลเห็น |

> **เหตุผลที่ต้องมีชั้นฐานข้อมูล:** ชั้นเว็บกันคนพิมพ์ผิด ชั้น API กันคนที่ยิงคำสั่งข้ามหน้าเว็บ
> ส่วนชั้นฐานข้อมูลกันกรณีที่เหลือทั้งหมด — สคริปต์นำเข้าข้อมูล การแก้ผ่าน console
> หรือ endpoint ที่เขียนเพิ่มในอนาคตแล้วลืมใส่การตรวจสอบ
> มีชุดทดสอบที่ยิงคำสั่ง SQL ค่าเกินเข้าฐานข้อมูลโดยตรงเพื่อพิสูจน์ว่าชั้นนี้ทำงานจริง

---

## 7. สิ่งที่ชุดทดสอบพิสูจน์ในเชิงวิชาการ

ชุดทดสอบอัตโนมัติทั้งหมด **422 ข้อ ผ่านทั้งหมด** รันกับ **PostgreSQL จริง** ไม่ใช่ฐานข้อมูลจำลอง
เพราะสิ่งที่ต้องพิสูจน์คือพฤติกรรมของ transaction และการล็อกแถวจริง

### 7.1 ACID ตอนซื้อรายวิชา (D9)

`WalletService.purchaseCourse()` ทำครบสี่ขั้นใน `prisma.$transaction` เดียว:
หักยอดผู้เรียน → บันทึกสิทธิ์เข้าเรียน → คำนวณส่วนแบ่ง → เพิ่มยอดผู้สอนและรายได้แพลตฟอร์ม

- **กันยอดติดลบ:** ล็อกแถวบัญชีด้วย `SELECT ... FOR UPDATE` **ก่อน**อ่านยอดไปเทียบกับราคา
  การล็อกเรียงตามรหัสบัญชีเสมอ เพื่อไม่ให้สองรายการที่แตะบัญชีชุดทับกันเกิด deadlock
- **กันซื้อซ้ำ:** ตัดสินด้วย unique index `(courseId, studentId)` ที่ระดับฐานข้อมูล
  การตรวจสอบด้วยโปรแกรมก่อนหน้าเป็นเพียงทางลัด ไม่ใช่ตัวกันจริง
- **กันยิงซ้ำ:** `LedgerTransaction.idempotencyKey` เป็น unique

### 7.2 ระบบบัญชีคู่ (double-entry ledger) (D2)

- ทุกธุรกรรมมีรายการบัญชีอย่างน้อย 2 แถว และ **ผลรวมด้านเดบิตเท่ากับด้านเครดิตเสมอ**
  ตรวจในโปรแกรมก่อน commit ทุกครั้ง และมีชุดทดสอบตรวจซ้ำท้ายเทสต์ทุกข้อที่แตะเงิน
- **ผลรวมยอดคงเหลือของทุกบัญชีในระบบเท่ากับศูนย์เสมอ** ใช้เป็นค่าคงที่ตรวจความถูกต้อง
- มีชุดทดสอบพิสูจน์ว่า **ผลรวมรายได้ผู้สอนทุกคน = ยอดค้างจ่ายที่ผู้ดูแลเห็น**
  และ **ผลรวมกราฟรายวัน = ยอดในการ์ดสรุป**
- รายการบัญชีเป็นแบบเพิ่มอย่างเดียว (append-only) แก้ผิดด้วยการบันทึกรายการกลับรายการเท่านั้น
- สคริปต์สร้างข้อมูลสาธิตตรวจงบทดลองก่อนจบทุกครั้ง (ครั้งล่าสุด: เดบิต 47,580.00 = เครดิต 47,580.00)

### 7.3 การบันทึกค่า ณ เวลาที่เกิดรายการ (snapshot) — สองตัว

ทั้งคู่ใช้หลักการเดียวกัน: **ค่าที่ตัดสินผลลัพธ์ต้องถูกบันทึกไว้ ณ เวลาที่เกิดเหตุ ไม่ใช่อ่านค่าปัจจุบันย้อนหลัง**

| snapshot | เก็บที่ | ป้องกันอะไร |
|---|---|---|
| `Enrollment.commissionRateSnapshot` | ทุกครั้งที่มีการซื้อ | ผู้ดูแลแก้อัตราส่วนแบ่งภายหลัง แล้วรายงานรายได้ย้อนหลังเปลี่ยนตาม |
| `QuizAttempt.passScoreSnapshot` | ทุกครั้งที่ทำข้อสอบ | ผู้สอนแก้เกณฑ์ผ่านภายหลัง แล้วผลสอบที่บันทึกไว้แล้วกลับจาก "ผ่าน" เป็น "ไม่ผ่าน" |

มีชุดทดสอบที่: ขายแล้วเปลี่ยนอัตราส่วนแบ่งแล้วขายอีกครั้ง → แถวเก่ายังแสดงอัตราเดิม ·
ทำข้อสอบผ่านที่เกณฑ์ 60 แล้วผู้สอนขึ้นเกณฑ์เป็น 70 → ผลเดิมยังเป็น "ผ่าน" แต่ผลใหม่ที่คะแนนเท่ากันเป็น "ไม่ผ่าน"

นอกจากนี้ยังมีชุดทดสอบที่ **อ่านซอร์สโค้ดทุกไฟล์** แล้วให้ผลเป็น "ไม่ผ่าน"
ถ้ามีใครเขียนโค้ดที่ตัดสินผลสอบเก่าจากเกณฑ์ปัจจุบันขึ้นมาใหม่ — เป็นการกันการถอยกลับ ไม่ใช่แค่ทดสอบพฤติกรรมปัจจุบัน

### 7.4 การควบคุมสิทธิ์ (RBAC) (D1)

- ตรวจสอง **ชั้นเสมอ**: ชั้นแรกตรวจบทบาท ชั้นที่สองตรวจความเป็นเจ้าของทรัพยากร
- ชุดทดสอบที่ยิง HTTP จริง พิสูจน์ว่า:
  ผู้สอน ก. ดูยอดขายของผู้สอน ข. ไม่ได้ · ดูรายชื่อผู้เรียนของ ข. ไม่ได้ ·
  สร้าง/แก้/ลบแบบทดสอบในคอร์สของ ข. ไม่ได้ทั้งสามกริยา · ผู้เรียนยิง endpoint ของผู้สอนไม่ได้ ·
  ไม่มี session เข้าไม่ได้เลย
- **ขอบเขตข้อมูลอยู่ในคำสั่งค้นหาระดับฐานข้อมูล ไม่ใช่การกรองผลลัพธ์ทีหลัง**
  จึงไม่มีรูปแบบการเรียกใดที่อ่านข้อมูลของคนอื่นได้
- ไม่มี response ใดที่ส่งรหัสผ่าน โทเคน หรืออีเมลของผู้ใช้คนอื่นออกไป มีชุดทดสอบตรวจ

### 7.5 Soft Lock (B7)

ไม่มีการบังคับลำดับการเรียนที่ใดในระบบ — ไม่มีการตรวจว่าเรียนบทก่อนหน้าจบหรือยัง
และไม่มีการตรวจว่าสอบผ่านหรือยังก่อนเข้าบทถัดไป ทั้งฝั่งเซิร์ฟเวอร์และหน้าจอ

### 7.6 การตรวจให้คะแนนอัตโนมัติ (D4)

- ตรวจที่เซิร์ฟเวอร์ทั้งหมด **เฉลยไม่เคยถูกส่งออกไปขณะที่ผู้เรียนกำลังทำข้อสอบ**
  โครงสร้างข้อมูลที่ส่งออกตอนทำข้อสอบไม่มีช่องสำหรับเฉลยตั้งแต่ระดับชนิดข้อมูล
- ผู้เรียนสองคนในรายวิชาเดียวกันได้คำถามและตัวเลือกชุดเดียวกัน เรียงเหมือนกันทุกประการ
  (พิสูจน์ว่าไม่มีการสุ่มจากคลังข้อสอบ)
- ทำซ้ำได้ไม่จำกัด เก็บทุกครั้ง ตัดสินด้วยคะแนนสูงสุด

---

## 8. ผลการตรวจ Responsive (D8)

ตรวจด้วยเบราว์เซอร์อัตโนมัติ (Playwright) ที่ความกว้าง **375 px (มือถือ) · 768 px (แท็บเล็ต) · 1440 px (เดสก์ท็อป)**
ครอบทุกหน้าหลักของทั้งสามบทบาท เกณฑ์การตรวจไม่ได้ดูแค่การล้นขอบจอ แต่รวมถึง
ปุ่มหรือคอลัมน์ที่ถูกดันออกนอกจอ และขนาดปุ่มที่เล็กเกินกว่าจะกดด้วยนิ้วที่ 375 px

**ขอบเขตการตรวจ:** 28 หน้าจอ × 3 ความกว้าง = **84 หน้าจอ**
(ผู้เรียน 10 หน้า · ผู้สอน 10 หน้า · ผู้ดูแลระบบ 8 หน้า)

**ผลการตรวจรอบสุดท้าย: ผ่านทั้ง 84 หน้าจอ** ไม่มีการล้นขอบจอ ไม่มีปุ่มหรือคอลัมน์ที่ถูกดันออกนอกจอ

**แนวทางที่ใช้:** ตารางที่มีหลายคอลัมน์จะ**ยุบเป็นการ์ดรายรายการ**ที่ความกว้างต่ำกว่า 768 px
แทนที่จะปล่อยให้เลื่อนแนวนอน เพราะตารางที่เลื่อนได้จะซ่อนคอลัมน์สำคัญไว้นอกจอ
เช่นคอลัมน์ "ยอดสุทธิ" ในตารางรายได้ ซึ่งเป็นคอลัมน์ที่ผู้สอนเปิดหน้านั้นมาดูโดยเฉพาะ

---

## 9. รายการที่จงใจไม่ทำ เพราะอยู่นอกขอบเขต ทก.01

รายการเหล่านี้ **ไม่ได้ตกหล่น** แต่ตรวจสอบแล้วว่าไม่มีข้อใดในเอกสาร ทก.01 กำหนดไว้
จึงไม่ได้ทำ และได้ตรวจยืนยันว่าไม่มีโค้ดหรือตารางค้างอยู่ในระบบ

| สิ่งที่ไม่ทำ | เหตุผล |
|---|---|
| **การถอนเงินของผู้สอน (payout)** | ทก.01 ข้อ A10 กำหนดเพียง "ดูยอดรายได้คงเหลือหลังหักส่วนแบ่ง พร้อมรายละเอียดการคำนวณ" ไม่ได้กำหนดการถอนเงินออกจากระบบ · enum `TxType` จึงมีเพียง `TOPUP` และ `PURCHASE` · ในรายงานผู้ดูแล คำว่า "ยอดค้างจ่ายผู้สอน" จึงหมายถึงรายได้สะสมทั้งหมด และมีข้อความกำกับไว้บนหน้าจอ |
| **คลังข้อสอบกลาง** | ทก.01 ข้อ D4 ระบุชัดว่าเป็น "ชุดคำถามเดียวที่ผู้เรียนทุกคนได้เหมือนกัน" · แบบทดสอบผูกกับบทเรียนแบบหนึ่งต่อหนึ่ง ไม่มีตารางคลังข้อสอบในฐานข้อมูล |
| **ข้อสอบอัตนัย** | ทก.01 ข้อ D4 ระบุว่า "ปรนัยเท่านั้น ตรวจให้คะแนนอัตโนมัติ" · โครงสร้าง `QuizQuestion` ไม่มีชนิดคำถามแบบเขียนตอบ |
| **Payment Gateway จริง** | ทก.01 ข้อ D3 ระบุว่าเป็น Mock Payment โดยผู้ดูแลยืนยันด้วยสายตา · ไม่มี dependency ของผู้ให้บริการชำระเงินรายใดในโปรเจกต์ |
| **ผู้ดูแลระบบหลายคน** | ทก.01 ข้อ C ระบุว่าเป็นบัญชีเดียวในขอบเขตนี้ · บัญชีผู้ดูแลสร้างได้จากสคริปต์ตั้งค่าเริ่มต้นเท่านั้น สมัครเองผ่านหน้าเว็บไม่ได้ |
| **ใบประกาศนียบัตร · ระบบรีวิว/ให้ดาว · ห้องเรียนสด · ระบบแนะนำคอร์ส · คูปอง · สมาชิกรายเดือน** | ไม่ปรากฏในเอกสาร ทก.01 · ตรวจสอบแล้วไม่มีตาราง เส้นทาง หรือหน้าจอใดในระบบ |

---

## 10. ผลการตรวจ Responsive โดยละเอียด

### 10.1 หน้าจอที่ตรวจ

| บทบาท | หน้าที่ตรวจ |
|---|---|
| ผู้เรียน | หน้าแรก · แคตตาล็อก · แคตตาล็อกที่กรองแล้ว · รายละเอียดคอร์ส · รายวิชาของฉัน · กระเป๋าเงิน · เติมเงิน · โปรไฟล์ · ห้องเรียน · กระดานถาม-ตอบ |
| ผู้สอน | ภาพรวม · แก้ไขคอร์ส (แท็บข้อมูลทั่วไป / เนื้อหาบทเรียน / แบบทดสอบ / ผู้เรียน) · สร้างคอร์สใหม่ · รายงานยอดขาย · กล่องคำถาม · กระเป๋าเงิน · โปรไฟล์ |
| ผู้ดูแลระบบ | ภาพรวม · คิวตรวจสลิป · อนุมัติคอร์ส · รายการที่ถูกแจ้ง · จัดการผู้ใช้ · หมวดหมู่ · รายงาน · โปรไฟล์ |

### 10.2 ปัญหาที่พบและแก้แล้ว

รอบแรกพบข้อบกพร่องเชิงโครงสร้าง **5 จุด ทุกจุดเป็นอาการเดียวกัน**:
ตารางที่มีคอลัมน์มากเกินกว่าจะพอดีจอเล็ก และคอลัมน์ที่หลุดออกไปคือ**คอลัมน์ปุ่มสั่งการ**

| หน้าจอ | สิ่งที่หลุดออกนอกจอ | ความกว้างที่พบ |
|---|---|---|
| `/admin/topups` | ปุ่ม "ตรวจสอบ" | 375, 768 |
| `/admin/users` | ปุ่ม "ระงับบัญชี" และ "แก้ไขส่วนแบ่ง" | 375, 768 |
| `/admin/categories` | ปุ่ม "แก้ไข" และ "ลบ" | 375 |
| `/instructor` | ปุ่ม "จัดการ" ของแต่ละคอร์ส | 375, 768 |
| `/wallet/topup` | ลิงก์ "ดูสลิป" และปุ่ม "ยกเลิกคำขอ" — และทำให้**ทั้งหน้ากว้าง 543 px ในจอ 375 px** | 375 |

**แก้ทั้งห้าจุดด้วยวิธีเดียวกัน:** ยุบเป็นการ์ดรายรายการเมื่อจอแคบ แทนการปล่อยให้เลื่อนแนวนอน
เป็นรูปแบบเดียวกับที่ตารางรายได้ผู้สอนและตารางรายชื่อผู้เรียนใช้อยู่แล้ว
ตรวจซ้ำหลังแก้: ทั้งห้าหน้าผ่านครบทุกความกว้าง

### 10.3 ข้อสังเกตที่เหลือ (ไม่ถือว่าไม่ผ่าน)

เครื่องมือตรวจตั้งเกณฑ์ความสูงของปุ่มไว้ที่ 36 px ซึ่งเข้มกว่ามาตรฐานทั่วไป จึงรายงานรายการต่อไปนี้
ทั้งหมดเป็น**ลิงก์ข้อความหรือปุ่มไอคอนรอง ไม่ใช่ปุ่มสั่งการหลัก** และยังกดใช้งานได้:

- ลิงก์ "กลับไปหน้า…" ที่ด้านบนของหลายหน้า (สูง 20 px)
- ลิงก์ "แจ้งเนื้อหาไม่เหมาะสม" ในหน้ารายละเอียดคอร์ส (สูง 16 px)
- ลิงก์ "ดูคะแนนรายชุด" ในตารางรายชื่อผู้เรียน (สูง 16 px)
- ปุ่มลากจัดลำดับบทเรียนในแท็บเนื้อหา (24 × 24 px)

ในทุกหน้าที่กล่าวถึง **ปุ่มสั่งการหลักเป็นปุ่มเต็มความกว้างหรือขนาดมาตรฐาน** เช่นปุ่ม "เติมเงินเพื่อซื้อ"
ในหน้ารายละเอียดคอร์ส และปุ่ม "บันทึกแบบทดสอบ" ในฟอร์มสร้างข้อสอบ

---

## 11. รายการที่ยังต้องตัดสินใจก่อนสอบ

รายการเหล่านี้ **ไม่กระทบความครบถ้วนตามข้อ A1–D9** แต่ควรตัดสินก่อนวันสอบ

### 11.1 Docker — ข้อ 2.7.4 ของ ทก.01

**สถานะปัจจุบัน:** `docker-compose.yml` ยกเฉพาะเครื่องแม่ข่ายเบื้องหลัง —
PostgreSQL, MinIO (object storage) และ Mailhog (เครื่องแม่ข่ายอีเมลสำหรับทดสอบ)
ส่วนตัวแอปพลิเคชัน (NestJS และ Next.js) รันบนเครื่องพัฒนาโดยตรง
มีคอมเมนต์กำกับไว้ในไฟล์ว่าตั้งใจเช่นนั้น แต่ยังไม่มี Dockerfile ของทั้งสองส่วน

**ทางเลือก:**

| ทางเลือก | ได้อะไร | เสียอะไร |
|---|---|---|
| **ก. เพิ่ม Dockerfile แล้วใส่ profile แยกใน compose** (แนะนำ) | `docker compose --profile full up` ยกทั้งระบบด้วยคำสั่งเดียว ตรงตามเอกสาร · การพัฒนาประจำวันยังรันบน host เหมือนเดิม ไม่ช้าลง | ต้องเขียน multi-stage build ที่รองรับ pnpm workspace เพิ่มอีกราวสองไฟล์ |
| ข. ย้ายทุกอย่างเข้า Docker ทั้งหมด | ตรงตามเอกสารที่สุด | การพัฒนาช้าลงมาก (rebuild ทุกครั้งที่แก้โค้ด) และไม่จำเป็นสำหรับงานขนาดนี้ |
| ค. อธิบายในเล่มว่า Docker ทำหน้าที่เป็นเครื่องแม่ข่ายทดสอบของฐานข้อมูล พื้นที่จัดเก็บไฟล์ และอีเมล | ไม่ต้องแก้โค้ดเลย | ถ้ากรรมการอ่านข้อ 2.7.4 ตามตัวอักษร อาจถูกถามว่าทำไมแอปไม่อยู่ใน Docker |

### 11.2 เรื่องปฏิบัติการ

| เรื่อง | รายละเอียด |
|---|---|
| **`prisma migrate reset` ต้องให้ผู้ใช้รันเอง** | Prisma บล็อกคำสั่งนี้เมื่อผู้สั่งเป็น AI · ถ้าจะล้างฐานข้อมูลตั้งต้นใหม่ก่อนสอบ ต้องรัน `pnpm db:reset` ด้วยตนเอง |
| **ห้ามรัน `pnpm build` ขณะที่ `pnpm dev` ทำงานอยู่** | แคช `.next` จะเสียหายและหน้าเว็บขึ้น 500 · แก้ด้วยการหยุด dev server ลบโฟลเดอร์ `frontend/.next` แล้วเริ่มใหม่ · พบระหว่างการตรวจครั้งนี้สองครั้ง |
| **สคริปต์ตรวจ Responsive ยังไม่ได้เก็บเข้า repo** | ปัจจุบันเป็นสคริปต์ชั่วคราว ถ้าต้องการรันซ้ำก่อนวันสอบ ควรเก็บเข้า repo เป็น `pnpm test:responsive` |
| **ยังไม่มีอีเมลแจ้งผลการตรวจสลิป** | ผู้เรียนต้องเปิดหน้ากระเป๋าเงินดูเอง · ทก.01 ไม่ได้กำหนดไว้ จึงไม่ถือว่าขาด แต่เป็นสิ่งที่กรรมการอาจถามถึง |
| **ยังไม่มีการแจ้งเตือนภายในเว็บ** | ผู้สอนต้องเปิดกล่องคำถามเอง · ทก.01 ไม่ได้กำหนดไว้เช่นกัน |
| **ปุ่มลากจัดลำดับบทเรียนเล็กบนมือถือ (24 × 24 px)** | ใช้งานได้แต่กดยาก · การจัดลำดับบทเรียนบนมือถือไม่ใช่งานหลัก จะขยายหรือไม่ก็ได้ |

---

## 12. ข้อมูลสำหรับตรวจสอบย้อนกลับ

| หัวข้อ | ที่อยู่ |
|---|---|
| ค่าคงที่ทุกตัวที่เอกสารกำหนด | `packages/shared/src/limits.ts` |
| ตรรกะบัญชีคู่ | `backend/src/modules/ledger/ledger.service.ts` |
| การซื้อคอร์สแบบ ACID | `backend/src/modules/ledger/wallet.service.ts` |
| การตัดสินสิทธิ์ชั้นที่สอง | `backend/src/modules/courses/course-access.service.ts` |
| การคำนวณความก้าวหน้า (จุดเดียวทั้งระบบ) | `backend/src/common/progress.ts` |
| การตัดสินผ่าน/ไม่ผ่าน (จุดเดียวทั้งระบบ) | `backend/src/common/quiz-scoring.ts` |
| ค่าคงที่ตรวจความถูกต้องของบัญชี | `backend/test/invariants.ts` |
| บันทึกการตัดสินใจเชิงเทคนิคทั้งหมด | `CLAUDE.md` |
