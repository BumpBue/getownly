# MIGRATION_LOG.md — แปลงดีไซน์ Google Stitch เป็น React component

บันทึกการแปลงดีไซน์จาก `design-reference/stitch_getownly_education_marketplace_ui/`
มาเป็นหน้าจริงในโปรเจกต์ · อ่านคู่กับ [CLAUDE.md](CLAUDE.md) และ [PLAN.md](PLAN.md)

---

## กติกาที่ใช้ร่วมกันทุกหน้า (ตกลงกับผู้ใช้แล้ว)

1. **สียึด token ของโปรเจกต์เป็นตัวตั้ง ไม่ใช่ค่าใน HTML ต้นฉบับ**
   ไฟล์ดีไซน์ 6 จาก 10 ไฟล์ใช้ชุดสีเก่า (`primary: #022445` · `secondary: #795823`)
   ส่วนอีก 4 ไฟล์ที่ลงท้าย `_updated_colors` ใช้ชุดใหม่ที่ถูกต้อง (`#1E3A5C` · `#C9A063`)
   ต้นตอคือ `prestige_thai_academy/DESIGN.md` ที่ยังไม่ได้อัปเดต
   **ทุกหน้าแปลงโดยใช้ `bg-primary` / `text-secondary` ของโปรเจกต์เสมอ**
   ผลคือ 6 หน้านั้นจะดูอ่อนกว่า `screen.png` เล็กน้อย ซึ่งเป็นความต่างที่ตั้งใจ
2. **ตัด gradient และเงาหนาทั้งหมด** (ข้อห้าม 18) ใช้ `border-border` + ระยะห่างแทน
3. **ไอคอนใช้ lucide-react** ซึ่งโปรเจกต์ใช้อยู่แล้ว ไม่เพิ่ม Tabler (ข้อห้าม 12)
   แมป Material Symbols → lucide ตามความหมายใกล้เคียงที่สุด
4. **เก็บ logic เดิมไว้ทั้งหมด** — fetch, state, error/loading/empty handling
   ห้ามลบฟังก์ชันที่ทำงานอยู่จริงเพียงเพราะดีไซน์ไม่ได้วาดไว้

---

## หน้า 1 — landing_page → `/`

**สถานะ:** เสร็จ · typecheck ผ่าน · lint ผ่าน · build ผ่าน · screenshot ตรวจแล้ว

### สิ่งที่เปลี่ยน

- **สร้าง `components/shared/SectionHeading.tsx`** — แถบทอง 4px คั่นหน้าหัวข้อ
  ซึ่งเป็นลายเซ็นของดีไซน์ชุดนี้ ทำเป็นคอมโพเนนต์เดียวเพราะจะถูกใช้ซ้ำทุกหน้า
  รับ `title` `subtitle` `action` (ปุ่มท้ายหัวข้อ) และ `as` สำหรับระดับ heading
- เปลี่ยนหัวข้อทั้ง 4 section ให้ใช้ `SectionHeading` — ของเดิมเป็นหัวข้อ**กึ่งกลาง**
  ตอนนี้เป็น**ชิดซ้าย + แถบทอง** ตามต้นฉบับ
- การ์ดหมวดหมู่เป็นสี่เหลี่ยมจัตุรัสตั้งแต่ `sm` ขึ้นไป ไอคอน `size-12` → `size-14`
  บนมือถือปล่อยความสูงอิสระ เพราะสี่เหลี่ยมจัตุรัส 4 ใบเรียงกันจะยาวเกินหนึ่งจอ
- `CourseCard` ได้ท้ายการ์ดแบบดีไซน์ — ราคามีเส้นทองคั่นซ้าย 4px
  พร้อมลูกศร `ArrowRight` ที่เลื่อนขวาตอน hover
  **คอร์สฟรีใช้เส้นสี `success` ไม่ใช่ทอง** เพราะแถบควรเห็นด้วยกับคำที่มันยืนข้างๆ
  ไม่ใช่ไปตั้งราคาให้ของที่ไม่มีราคา

### ความต่างจาก `screen.png`

- **ไม่มี gradient ใน hero และไม่มีเงาใต้การ์ด** — ต้นฉบับมี
  `bg-[radial-gradient(...)]`, `shadow-2xl`, `shadow-[0_4px_20px_rgba(30,58,92,0.08)]`
  ทั้งหมดชนข้อห้าม 18 การ์ดจึงดู flat กว่าภาพต้นฉบับ **นี่คือความต่างที่เห็นชัดที่สุด**
- ต้นฉบับใช้รูปถ่ายจาก `lh3.googleusercontent.com` ใน hero
  ผมเก็บ `HeroCourseMockup` ของเดิมไว้ เพราะเป็นคอมโพเนนต์ที่ทำงานได้จริงอยู่แล้ว
  ไม่ใช่ภาพประกอบที่ต้องหามาแทน

### สิ่งที่ยังไม่ได้ทำ

- **bottom nav bar บนมือถือ** — ยกไปทำเป็น layout ของกลุ่ม `(student)` ในหน้า student_dashboard
  เพื่อไม่ให้ได้ nav สองแบบที่ไม่ตรงกัน

### ข้อสังเกตที่ไม่เกี่ยวกับดีไซน์

- **ภาพปกคอร์สทุกใบ 404** — `prisma/seed.ts` เขียน `coverKey` เป็น
  `cover/<key>/cover.jpg` แต่**ไม่เคยอัปโหลดไฟล์จริงขึ้น MinIO**
  เป็นข้อจำกัดของ seed ที่มีมาก่อนงานนี้ ไม่ได้เกิดจากการแปลงดีไซน์
  `CourseCard` มี fallback `ImageOff` อยู่แล้วแต่ไม่ทำงาน เพราะ `coverUrl` ไม่ใช่ null
  (URL เซ็นได้สำเร็จ แต่ object ปลายทางไม่มีอยู่)

---

## หน้า 2 — login_screen → `/login`

**สถานะ:** เสร็จ · typecheck ผ่าน · screenshot ตรวจแล้ว

### สิ่งที่เปลี่ยน

- **สร้าง `components/ui/password-input.tsx`** — ปุ่มแสดง/ซ่อนรหัสผ่าน
  แปลง `onclick` ของ vanilla JS ในต้นฉบับเป็น React state (`useState`)
  **ปุ่มตั้ง `tabIndex={-1}` โดยตั้งใจ** เพราะคนกรอกฟอร์มด้วยคีย์บอร์ดจะ tab
  จากช่องรหัสผ่านไปปุ่มส่งข้อมูล การมีจุดแวะกลางทางที่ไม่มีใครขอคือสะดุด
  บนเส้นทางที่ทุกคนต้องเดิน · เมาส์และ screen reader ยังเข้าถึงได้ผ่าน `aria-label`
  ซึ่งสลับตามสถานะเพื่อบอกว่า*กดแล้วจะเกิดอะไร* ไม่ใช่บอกว่าตอนนี้เป็นอะไร
- **เพิ่ม prop `labelAction` ให้ `components/ui/field.tsx`** — ช่องท้ายแถว label
  ดีไซน์วาง "ลืมรหัสผ่าน" ไว้แถวเดียวกับ label ของรหัสผ่าน ไม่ใช่ลอยอยู่ใต้ช่อง
  ทางออกฉุกเฉินจึงอยู่ตรงที่ปัญหาเกิด · เปลี่ยนสีลิงก์เป็น `secondary` ตามต้นฉบับ
- **แถบทองใต้ชื่อแบรนด์**ในแผงซ้ายของ `(auth)/layout.tsx`
  ดีไซน์ขีดเส้นทอง*ใต้*ชื่อแบรนด์ ต่างจาก `SectionHeading` ที่ตั้งแถบไว้*ข้าง*หัวข้อ
  จึงเขียนตรงในเลย์เอาต์ ไม่ยัดเข้า `SectionHeading` ซึ่งเป็นคนละท่า

### ความต่างจาก `screen.png`

- **ไม่มีปุ่ม "เข้าสู่ระบบด้วย Google"** ซึ่งต้นฉบับมี พร้อมเส้นคั่น "หรือ"
  โปรเจกต์นี้ออก JWT เอง ไม่มี OAuth และ CLAUDE.md หัวข้อ 2 ระบุไว้ว่า
  "ไม่ต้องพึ่ง auth provider ภายนอก" · ข้อห้าม 12 ห้ามเพิ่ม auth provider ภายนอก
  และ PLAN.md ไม่มี Google login ในขอบเขต · **ปุ่มที่กดแล้วไม่เกิดอะไรแย่กว่าไม่มีปุ่ม**
  จึงตัดทั้งปุ่มและเส้นคั่นออก ไม่ใช่การลบฟีเจอร์ที่ทำงานอยู่ แต่คือการไม่สร้างของที่ไม่มีจริง
- ไม่มี gradient/blur ตกแต่ง (`blur-3xl` วงกลมสองวง + `bg-gradient-to-t`) และไม่มี
  รูปประกอบใน hero ซ้าย · ต้นฉบับมีทั้งหมดนี้ ตัดตามข้อห้าม 18
- ต้นฉบับครอบฟอร์มด้วยการ์ดที่มีเงา ผมใช้ฟอร์มเปล่าบนพื้นขาวตามของเดิม
  เพราะแผงขวาเป็น `bg-card` อยู่แล้ว การ์ดขาวบนขาวจะเห็นได้ด้วยเงาเท่านั้น

### logic ที่รักษาไว้ครบ

`react-hook-form` + `zodResolver`, `applyApiError`, การอ่าน `?next=` ตอน submit
(ไม่ใช้ `useSearchParams` เพราะจะทำให้ทั้งหน้าเป็น client-only), การกัน
protocol-relative URL (`//`), `HOME_PATH_BY_ROLE`, สถานะ `isSubmitting`, `Alert` ระดับฟอร์ม

---

## หน้า 3 — register_screen → `/register`

**สถานะ:** เสร็จ · typecheck ผ่าน · screenshot ตรวจแล้วทั้ง 2 ขั้นตอน

### สิ่งที่เปลี่ยน

- **แถบความคืบหน้า 2 ช่อง** (`RegisterProgress`) ตามต้นฉบับ ช่องแรกทองเสมอ
  ช่องสองทองเมื่อถึงขั้นที่ 2 · ใส่ `role="progressbar"` + `aria-valuenow` ให้ screen reader
  การสมัครสมาชิกเป็น flow เดียวในระบบที่ถามอะไรบางอย่างก่อนจะยอมแสดงฟอร์มจริง
  จึงเป็นที่เดียวที่ต้องสัญญาว่ารออีกไม่นาน
- **การ์ดบทบาทมีกล่องไอคอนสีต่างกัน** — ผู้เรียนใช้ `primary/10` ผู้สอนใช้ `secondary/15`
  ตามต้นฉบับที่แยกสองประตูออกจากกันด้วยสี ไม่ใช่รายการเดียวสองแถว
- **ปุ่มย้อนกลับแบบลูกศร** แทนกล่อง "เปลี่ยนบทบาท" เดิม แต่**คงชื่อบทบาทที่เลือกไว้ในป้าย**
  ต้นฉบับเขียนแค่ "ย้อนกลับ" ลอยๆ · สิ่งเดียวที่ตัดสินใจไปแล้วควรยังมองเห็นระหว่างกรอกที่เหลือ
- ช่องรหัสผ่านทั้งสองช่องใช้ `PasswordInput` ที่สร้างไว้ในหน้า 2

### ความต่างจาก `screen.png`

- **การ์ดบทบาทกดแล้วไปขั้นถัดไปทันที ไม่มีปุ่ม "ดำเนินการต่อ"**
  ต้นฉบับใช้ radio + ปุ่มยืนยันแยก = 2 คลิก · ของเดิมในโปรเจกต์เป็น 1 คลิกซึ่งดีกว่า
  และเป็นพฤติกรรมที่ทำงานอยู่แล้ว · ผมจึงคงไว้ แล้วใส่**ลูกศร**แทน**เครื่องหมายถูก**
  เพราะติ๊กถูกจะเป็นจริงแค่เฟรมเดียวก่อนหน้าจอเปลี่ยน แต่ลูกศรบอกตรงๆ ว่ากดแล้วจะเกิดอะไร
- **ไม่มีช่อง "ชื่อ" / "นามสกุล" แยกกัน** ต้นฉบับแยกสองช่อง แต่ backend มี `displayName`
  ช่องเดียว (`POST /auth/register`) การแยกช่องจะต้องแก้ DTO และ schema ซึ่งเกินขอบเขตงานหน้าตา
- **ไม่มี checkbox "ยอมรับข้อตกลง"** ของเดิมใช้ประโยคกำกับใต้ปุ่มแทน (`t.terms`)
  ซึ่งให้ผลทางกฎหมายเหมือนกันแต่ไม่เพิ่มด่านให้กด · การเพิ่ม checkbox บังคับต้องมี
  validation ใหม่ใน `registerSchema` และไม่มีหน้าข้อตกลงจริงให้ลิงก์ไป
- ต้นฉบับมี header/footer ของตัวเองในหน้านี้ · โปรเจกต์ใช้ `(auth)/layout.tsx`
  แบบสองแผงร่วมกันทั้ง 4 หน้า auth ซึ่งขัดกับต้นฉบับหน้านี้ที่มี navbar เต็ม
  **เลือกตามเลย์เอาต์ร่วม** เพราะดีไซน์ login/forgot ใช้สองแผงแบบไม่มี navbar
  ถ้าทำตามหน้านี้หน้าเดียวจะได้ auth 4 หน้าที่หน้าตาไม่เป็นชุดเดียวกัน

### logic ที่รักษาไว้ครบ

`?role=instructor` อ่านฝั่ง server ใน `page.tsx` (ไม่ใช้ `useSearchParams`),
`SELF_SERVICE_ROLES` validation, `registerSchema` + `zodResolver`, `applyApiError`,
`HOME_PATH_BY_ROLE` หลังสมัครสำเร็จ, `isSubmitting`

---

## หน้า 4 — forgot_password → `/forgot-password` (+ `/reset-password`)

**สถานะ:** เสร็จ · typecheck ผ่าน · lint ผ่าน · screenshot ตรวจแล้ว

### สิ่งที่เปลี่ยน

- **`AuthFormHeader` รับ prop `icon` / `tone` / `align`** — ป้ายไอคอนเหนือหัวข้อตามต้นฉบับ
  ใส่เฉพาะหน้าที่มาถึงแบบไม่มีบริบทรอบข้าง (ลืมรหัสผ่าน / ตั้งรหัสผ่านใหม่)
  เพราะคนกดมาจากลิงก์ในอีเมลตรงๆ ไอคอนบอกว่านี่คือหน้าประเภทไหนก่อนจะได้อ่านประโยค
  **หน้า login/register ไม่ใส่** เพราะเดินมาถึงเองอย่างตั้งใจอยู่แล้ว
- **`Input` รับ prop `icon`** — ไอคอนนำหน้าในช่องกรอกตามต้นฉบับ (`Mail` ในช่องอีเมล)
  เป็นแค่ป้ายให้ตาดู ตั้ง `aria-hidden` ไว้ ไม่ได้แทน label ที่ screen reader อ่าน
- **เพิ่มข้อความเตือนเรื่องโฟลเดอร์จดหมายขยะ** (`spamHint`) ในหน้ายืนยันการส่ง ตามต้นฉบับ
  ของเดิมไม่มี · เป็นข้อมูลที่ช่วยคนที่หาอีเมลไม่เจอจริงๆ
- **ลิงก์ "กลับไปหน้าเข้าสู่ระบบ" มีลูกศรที่เลื่อนซ้ายตอน hover** ตามต้นฉบับ
- **`/reset-password` ได้ชุดเดียวกัน** — ป้ายไอคอน 3 สถานะ (`AlertTriangle` เมื่อ token หาย ·
  `KeyRound` ตอนกรอก · `CheckCircle2` เขียวตอนสำเร็จ) และช่องรหัสผ่านทั้งสองใช้ `PasswordInput`
  ต้นฉบับไม่มีหน้านี้ แต่มันอยู่ในตระกูล auth เดียวกัน ปล่อยไว้จะกลายเป็นหน้าที่หน้าตาไม่เข้าพวก

### ความต่างจาก `screen.png`

- **ไม่มีการ์ดลอยกลางจอพร้อมเงาและแถบทองด้านบน** ต้นฉบับหน้านี้เป็นการ์ดเดี่ยวกลางจอ
  บนพื้นลายจุด มี `card-elevation` (เงา) + `border-t-4 border-secondary`
  ผมใช้เลย์เอาต์สองแผงของ `(auth)/layout.tsx` ร่วมกับอีก 3 หน้า auth
  **เหตุผลเดียวกับหน้า register** — ถ้าทำตามต้นฉบับทีละหน้าจะได้ auth 4 หน้าที่ไม่เป็นชุดเดียวกัน
  ส่วนเงาและพื้นลายจุด/แสงเรืองก็ชนข้อห้าม 18 อยู่แล้ว
- ต้นฉบับเปลี่ยนสีขอบการ์ดเป็นเขียวตอนส่งสำเร็จ · ผมใช้ป้ายไอคอนสีเขียวแทน
  ซึ่งสื่อความเดียวกันโดยไม่ต้องมีขอบการ์ดให้เปลี่ยน

### logic ที่รักษาไว้ครบ

การแยกสองมุมมอง (ฟอร์ม / ยืนยันการส่ง) ซึ่งเป็น React state อยู่แล้ว
ไม่ใช่ `classList.add('hidden')` แบบต้นฉบับ · `forgotPasswordSchema`,
`applyApiError`, `isSubmitting` · **คำตอบจาก API เหมือนกันไม่ว่าอีเมลจะมีอยู่จริงหรือไม่**
หน้าจึงไม่ยืนยันว่าเจอบัญชี (คอมเมนต์เดิมยังอยู่) · `/reset-password` ยังอ่าน token
ด้วย `useSearchParams` + skeleton fallback ตามเดิม

---

## หน้า 5 — student_dashboard → `/home` (+ BottomNav ทั้งระบบ)

**สถานะ:** เสร็จ · typecheck ผ่าน · lint ผ่าน · screenshot ตรวจแล้วทั้ง desktop และ 375px

### ⚠️ ดีไซน์ต้นฉบับหน้านี้ขัดแย้งในตัวเอง

`student_dashboard_desktop/code.html` มี **sidebar ของ ADMIN** อยู่ในหน้าของผู้เรียน
(เขียนว่า "Admin Portal" · "Management Console" · "Create New Course" · Reports · Analytics · Settings)
และเนื้อหาส่วนใหญ่เป็นภาษาอังกฤษ (Assignments · Discussions · Explore · Continue Learning)
ซึ่งชนข้อห้าม 19 · ชัดเจนว่าเป็นความผิดพลาดตอน generate ของ Stitch ไม่ใช่เจตนาออกแบบ

**ไม่ได้หยุดถาม** เพราะผู้ใช้สั่งไว้ล่วงหน้าแล้วว่ากลุ่ม student ใช้ bottom nav บนมือถือ
และ **ไม่ใช้ sidebar** (sidebar สงวนไว้ให้ instructor/admin) จึงถือว่าข้อขัดแย้งนี้ถูกตัดสินไปแล้ว
ข้อความทั้งหมดแปลเป็นไทยตามข้อห้าม 19

### สิ่งที่เปลี่ยน

- **สร้าง `components/shared/BottomNav.tsx`** — แถบล่างบนมือถือ 5 เมนู
  (หน้าแรก · ค้นหา · คอร์สของฉัน · กระเป๋าเงิน · โปรไฟล์)
  **แสดงเฉพาะ `role === "STUDENT"` ที่ล็อกอินแล้ว และเฉพาะจอ < `md`**
  อ่าน session เองด้วย `me({ silentOn401: true })` แบบเดียวกับ `MainNav`
  เพราะมันเรนเดอร์อยู่ใน 3 route group ที่ไม่มีกลุ่มไหนรู้ role
  · ผู้สอนและ ADMIN ถูกกันออกทั้งที่เข้าบาง route ได้ เพราะทั้งคู่มี sidebar เป็นทางเดินอยู่แล้ว
  การมี nav ถาวรตัวที่สองที่เสนอปลายทางคนละชุดจะทำให้คำถาม "ตอนนี้อยู่ไหน" มีสองคำตอบ
  · guest ถูกกันออกเพราะ 4 ใน 5 ปลายทางจะเด้งไป `/login` อยู่ดี
- **ต่อเข้า 3 layout: `(student)` `(account)` `(public)`** ไม่ได้ก๊อปลงทีละหน้า
  ทั้งสามใส่ `pb-16 md:pb-0` เพื่อเว้นที่ให้แถบที่ลอยทับอยู่
  ใน `(public)` ใส่ที่ตัวห่อชั้นนอกไม่ใช่ `<main>` เพราะ footer ต้องพ้นแถบด้วย
- **`/courses` active ไม่ติดเมื่ออยู่ `/courses/[id]`** (เทียบ `pathname === item.href` ตรงๆ)
  หน้าคอร์สคือที่ที่เดินมาจากการค้นหา ไม่ใช่ตัวแท็บค้นหาเอง
- **การ์ดปุ่มลัดเป็นแบบ bento ตามต้นฉบับ** — ไอคอนกับสถานะอยู่แถวบน ชื่ออยู่ล่างสุด
  `min-h-40` ให้ทุกใบสูงเท่ากัน · การ์ด "เรียกดูคอร์สทั้งหมด" เป็น**การ์ดกลับสี**
  (พื้น `primary` + เส้นทองด้านบน) ตามการ์ด "Explore" ของต้นฉบับ · มีใบเดียวต่อแถว
  จึงทำหน้าที่เป็นทางออกไปแคตตาล็อก ไม่ใช่ของประดับ · ไอคอนเปลี่ยน `BookOpen` → `Compass`
- **`Progress` รับ prop `tone`** — `secondary` (ทอง) สำหรับความคืบหน้าการเรียนตามต้นฉบับ
  ส่วนความคืบหน้าการอัปโหลดยังเป็น `primary` เพราะทองสื่อถึงความสำเร็จ
  และไฟล์ที่กำลังวิ่งผ่านสายไม่ใช่ความสำเร็จ
- **ป้าย "กำลังเรียน" ลอยบนภาพปก** ในการ์ดเรียนต่อ ตามต้นฉบับ
- หัวข้อ "เรียนต่อ" ใช้ `SectionHeading` (แถบทอง) · ต้นฉบับใช้เส้นทองใต้หัวข้อแทน
  เลือกแถบข้างเพื่อให้ตรงกับอีก 8 หน้าที่เหลือ

### ความต่างจาก `screen.png`

- **ไม่มี sidebar** ตามที่อธิบายข้างบน
- **ไม่มีการ์ด "Assignments" และ "Discussions"** ระบบยังไม่มีงานที่มอบหมาย
  และกระดานถาม-ตอบเข้าจากในห้องเรียน ไม่ใช่จากหน้าแรก · การ์ดที่ลิงก์ไปที่ไม่มีอยู่คือทางตัน
- การ์ดปุ่มลัดแสดงตาม role จริง (ผู้สอนเห็นแดชบอร์ดผู้สอน · ADMIN เห็นคิวงานค้าง)
  ซึ่งต้นฉบับไม่มี เพราะต้นฉบับสมมติว่าเป็นหน้าของผู้เรียนอย่างเดียว
- ไม่มีเงา (`custom-shadow`, `hover-lift` ที่มี `box-shadow`) ตามข้อห้าม 18

### logic ที่รักษาไว้ครบ

`me()` → โหลดตาม role (`getInstructorOverview` สำหรับ INSTRUCTOR ·
`listTopupsForAdmin` + `listPendingCourses` สำหรับ ADMIN · `listMyEnrollments` ทุกคน),
สถานะ loading (skeleton) / error (`EmptyState` + ปุ่มลองใหม่) / empty,
การเลือก `inProgress` จาก `progressPercent > 0 && < 100`, ทักทายตามเวลาไทย

---

## หน้า 6 — course_catalog → `/courses`

**สถานะ:** เสร็จ · typecheck ผ่าน · lint ผ่าน · screenshot ตรวจแล้ว

### สิ่งที่เปลี่ยน

- หัวข้อหน้าใช้ `SectionHeading as="h1"` (แถบทอง)
- **ย้าย "เรียงลำดับ" ออกจาก sidebar ตัวกรอง ขึ้นมาอยู่แถบเหนือผลลัพธ์** ตามต้นฉบับ
  สร้าง `CourseSortSelect.tsx` · แถบนี้มีจำนวนผลลัพธ์อยู่ซ้าย เรียงลำดับอยู่ขวา
  คั่นด้วย `border-b` ตามต้นฉบับ
  **และเปลี่ยนเป็นทำงานทันทีที่เลือก ไม่ต้องกดปุ่ม "ใช้ตัวกรอง"**
  เพราะการเรียงลำดับไม่ใช่ตัวกรอง — มันไม่เคยเปลี่ยนว่ามีคอร์สอะไรอยู่ในชุดผลลัพธ์
  เปลี่ยนแค่ลำดับ จึงไม่มีอะไรให้ทบทวนก่อนยืนยัน · และถ้าคาไว้ในฟอร์ม
  ช่วงราคาที่พิมพ์ค้างครึ่งทางจะถูกส่งไปพร้อมกับการเรียงลำดับใหม่ด้วย
  · เปลี่ยนลำดับแล้ว**กลับไปหน้า 1 เสมอ** เพราะหน้า 3 ของลำดับเก่าไม่มีความหมายในลำดับใหม่
- ช่องค้นหาใน sidebar เปลี่ยนมาใช้ prop `icon` ของ `Input` ที่เพิ่มไว้ในหน้า 4
  แทนการวาง `<Search>` ด้วย `absolute` เอง — ลดโค้ดซ้ำและได้ระยะห่างตรงกับช่องอื่น

### ความต่างจาก `screen.png`

- **ไม่มีป้ายมุมซ้ายบนของภาพปก** (ต้นฉบับมีป้าย "ขายดี"/"ใหม่" สีพื้น `primary`)
  ระบบยังไม่มีข้อมูลว่าคอร์สไหนขายดีหรือใหม่ในเชิงป้าย · ป้ายที่ไม่มีข้อมูลจริงหนุนคือป้ายหลอก
  ป้าย "ฟรี" ที่มีอยู่แล้วยังอยู่ในตัวการ์ด
- **ไม่มีปุ่มวงกลมมุมขวาล่างของการ์ด** (ต้นฉบับเป็นปุ่มขอบวงกลมมีลูกศร)
  ผมใช้ลูกศรเปล่าตามที่ทำไว้ในหน้า 1 เพราะทั้งการ์ดเป็นลิงก์เดียวอยู่แล้ว
  ปุ่มซ้อนในลิงก์จะได้ nested interactive element ที่ screen reader อ่านสับสน
- **ตัวกรองราคาเป็นช่องกรอกตัวเลข ไม่ใช่ slider** ต้นฉบับมี `<input type="range">`
  ของเดิมเป็นช่องต่ำสุด–สูงสุดซึ่งระบุค่าที่แน่นอนได้ และ slider ต้องรู้ราคาสูงสุดในระบบก่อน
  จึงจะกำหนดขอบเขตได้ ซึ่งไม่มีใน API ตอนนี้
- ไม่มีเงา (`shadow-tier-2`) ตามข้อห้าม 18

### logic ที่รักษาไว้ครบ

**สถานะตัวกรองทั้งหมดยังอยู่ใน URL** หน้าจึงยังเป็น Server Component
กดย้อนกลับได้และแชร์ลิงก์ผลค้นหาได้ (คอมเมนต์เดิมยังอยู่) · `readFilters` ที่ทนต่อ
query string ที่แก้มือ · `buildQuery` · `sanitisePrice` · `freeOnly` ที่ปิดช่องราคา ·
การแบ่งหน้า · empty state · error state เมื่อ `serverFetch` คืน null

---

## หน้า 7 — course_detail → `/courses/[id]`

**สถานะ:** เสร็จ · typecheck ผ่าน · lint ผ่าน · screenshot ตรวจแล้ว

### สิ่งที่เปลี่ยน

- **`CardTitle` มีแถบทองคั่นหน้าเป็นค่าเริ่มต้น** (`h-5 w-1` เล็กกว่าของ `SectionHeading`
  ที่เป็น `h-8 w-1`) เพื่อให้อ่านเป็นเครื่องหมายเดียวกันที่ต่างระดับ ไม่ใช่แข่งกันเอง
  **เปิดเป็นค่าเริ่มต้นไม่ใช่ opt-in** เพราะต้นฉบับใส่ให้หัวข้อการ์ดทุกใบที่วาด
  accent ที่ต้องจำใส่เองทุกจุดเรียกใช้คือ accent ที่จะหายไปหนึ่งในสามของจุด
  มี `accent={false}` ไว้สำหรับหัวข้อที่ไม่ใช่จุดเริ่มของ section (เช่นใน modal)
  · **จุดนี้กระทบ `<CardTitle>` ทั้ง 14 จุดทั่วแอป** รวมหน้า instructor/admin ที่ยังไม่ได้แปลง
- **แผงซื้อคอร์สมีเส้นทองด้านบน** (`border-t-4 border-t-secondary`)
  เป็น accent เดียวกับที่หัวข้อใช้ แต่ยกมาเป็นขอบของแผงเอง
  เพื่อให้การ์ดใบเดียวที่รับเงินถูกทำเครื่องหมายว่าต่างจากการ์ดที่แค่บรรยาย
- **ราคาอยู่กึ่งกลาง ขนาดใหญ่ขึ้น** (`text-3xl` → `text-4xl`) พร้อมคำกำกับ
  "ซื้อครั้งเดียว เรียนได้ตลอดชีพ" ใต้ราคา ตามต้นฉบับ
- **แถวบทเรียนมี hover** และไอคอนเล่นเปลี่ยนเป็นสีทองตอน hover ตามต้นฉบับ

### ความต่างจาก `screen.png`

- **ไม่มีราคาเดิมขีดฆ่า** (ต้นฉบับโชว์ `฿ 5,900` ขีดฆ่าคู่กับ `฿ 3,500`)
  ระบบไม่มีแนวคิดราคาก่อนลด — `Course` มี `price` ค่าเดียว และส่วนลด/โปรโมชัน
  อยู่ใน "นอกขอบเขต" ของ PLAN.md หัวข้อ 0 · ราคาขีดฆ่าที่ไม่มีส่วนลดจริงคือการโฆษณาเท็จ
- **เนื้อหาหลักสูตรเป็นรายการแบน ไม่ใช่ accordion แยกบท**
  ต้นฉบับแบ่งเป็น "บทที่ 1 / บทที่ 2" ที่กางออกได้ · schema จริงไม่มี `Section`
  (ดู CLAUDE.md หัวข้อ 8 — เฟส 1 ตัด `Section` ออก `Lesson` ผูกกับ `Course` ตรงๆ)
  การทำ accordion จึงต้องแก้ฐานข้อมูล ซึ่งเกินขอบเขตงานหน้าตา
- ไม่มีเงา (`shadow-card`) ตามข้อห้าม 18

### logic ที่รักษาไว้ครบ — และพิสูจน์แล้วว่ายังทำงาน

`StickyPanel`, การแยก 3 กรณี (เจ้าของคอร์ส / ซื้อแล้ว / ยังไม่ซื้อ) และ `PurchasePanel`
**screenshot จับได้ว่ายอดกระเป๋า ฿1,620 น้อยกว่าราคา ฿1,790 แล้วขึ้นคำเตือน
"ยอดเงินในกระเป๋าไม่พอ ขาดอีก ฿170.00" พร้อมปุ่มเปลี่ยนเป็น "เติมเงินเพื่อซื้อ" ถูกต้อง**
ซึ่งพิสูจน์ว่า `money.ts` (`covers`/`shortfall`) และการดึงยอดกระเป๋าฝั่ง server ยังทำงานจริง

---

## หน้า 8 — wallet_top_up → `/wallet/topup`

**สถานะ:** เสร็จ · typecheck ผ่าน · lint ผ่าน · screenshot ตรวจแล้วทั้ง 3 ขั้นตอน

### สิ่งที่เปลี่ยน

- หัวข้อหน้าใช้ `SectionHeading as="h1"` · หัวข้อการ์ดทั้ง 4 ใบได้แถบทองอัตโนมัติ
  จากที่แก้ `CardTitle` ในหน้า 7
- **ปุ่มจำนวนเงินที่เลือกบ่อยเป็นตาราง 3 คอลัมน์** (`grid grid-cols-3`) ตามต้นฉบับ
  แทน `flex-wrap` เดิมที่ปุ่มกว้างไม่เท่ากันและตกบรรทัดไม่สม่ำเสมอ · ขนาดปุ่มโตขึ้นจาก `sm`
- **สัญลักษณ์ ฿ อยู่ในช่องกรอกจำนวนเงิน** ตามต้นฉบับ
- **เวลาหมดอายุ QR เป็นป้ายกลมสีทอง** ตามต้นฉบับ

### ความต่างจาก `screen.png`

- **ไม่ใช่นาฬิกานับถอยหลัง** ต้นฉบับแสดง "รอการชำระเงิน: 04:59" ที่เดินถอยหลัง
  ผมใช้**เวลาหมดอายุ**ในป้ายทองแบบเดียวกันแทน เพราะ CLAUDE.md หัวข้อ 8 ระบุไว้ชัดว่า
  quote **ไม่ได้จองอะไรไว้เลย** และ **QR ที่หมดอายุบนจอยังโอนเข้าได้ปกติ**
  นาฬิกาที่เดินไปถึงศูนย์จะสัญญาเส้นตายที่ไม่มีอยู่จริง แล้วผู้ใช้ที่โอนช้าจะคิดว่าเงินหาย
- ไม่มีลายน้ำตัวอักษรเอียงหลัง QR (`opacity-10 -rotate-45`) — เป็นของประดับที่
  ลดความคมของ QR ที่ต้องถูกสแกน
- ไม่มีเงา (`shadow-tier-2`) ตามข้อห้าม 18

### logic ที่รักษาไว้ครบ — และพิสูจน์แล้วว่ายังทำงาน

**screenshot จับได้ครบทั้งวงจร**: กด "สร้าง QR" → เรียก `POST /topups/quote` สำเร็จ →
ได้ QR PromptPay จริง + ป้ายเตือน "QR สำหรับสาธิตระบบ กรุณาอย่าโอนเงินจริง"
(ธง `isDemoMode` จาก API ไม่ใช่ env ฝั่งเว็บ) + ชื่อบัญชี + เลขพร้อมเพย์ +
ปุ่มคัดลอก/ดาวน์โหลด → กล่องลากสลิป → ปุ่ม "แจ้งการโอนเงิน" ที่ **disabled จนกว่าจะอัปโหลดสลิป**
· ประวัติการเติมเงินแสดงสถานะถูกต้อง (รอตรวจสอบ/อนุมัติแล้ว) พร้อมปุ่มยกเลิกเฉพาะรายการที่ยังรอ
· `loadHistory` · `cancelRequest` · การแบ่งหน้า · `submitTopup` ที่ใช้ `quote.amount`
ไม่ใช่ค่าจากช่องกรอก (คอมเมนต์เดิมยังอยู่)

---

## หน้า 9 — instructor_dashboard → `/instructor`

**สถานะ:** เสร็จ · typecheck ผ่าน · lint ผ่าน · screenshot ตรวจแล้ว

### ⚠️ ดีไซน์ต้นฉบับมีปัญหาเดียวกับหน้า student_dashboard

`instructor_dashboard_desktop/code.html` มี sidebar เขียนว่า **"Admin Portal /
Management Console"** เหมือนกันเป๊ะ และเมนูเป็น Dashboard/Management/Reports/Analytics/Settings
ซึ่งไม่ใช่เมนูของผู้สอน · เป็น template เดียวที่ Stitch ใช้ซ้ำทั้งสามหน้า dashboard
**ใช้ sidebar จริงของโปรเจกต์** (ภาพรวม / สร้างคอร์สใหม่ / กล่องคำถาม / รายงานยอดขาย)

### สิ่งที่เปลี่ยน

- **สร้าง `components/shared/SidebarLink.tsx`** — เมนู sidebar ที่**รู้ว่าตัวเองคือหน้าปัจจุบัน**
  ของเดิมไม่มี active state เลย · ใช้**เส้นทองด้านซ้าย** ตามต้นฉบับ
  (`border-l-4 border-secondary`) ซึ่งเป็น accent เดียวกับ `SectionHeading`/`CardTitle`
  แต่หันมาวางบนพื้นน้ำเงิน · **สีพื้นอ่อนอย่างเดียวไม่พอ** เพราะน้ำเงินอ่อนกว่านิดเดียว
  บนพื้น `primary` แทบมองไม่เห็น เส้นทองจึงเป็นตัวทำงานจริง ส่วนพื้นอ่อนเป็นตัวเสริม
  · มี prop `exact` สำหรับหน้ารากของกลุ่ม (`/instructor`, `/admin`) เพราะ path ของมัน
  เป็น prefix ของทุกเมนูอื่น ถ้าไม่มีจะติดสว่างค้างทุกหน้าแล้วเครื่องหมายจะเลิกแปลว่า "อยู่ตรงนี้"
- หัวข้อหน้าใช้ `SectionHeading as="h1"` พร้อม `action` เป็นปุ่ม "สร้างคอร์สใหม่"
- **การ์ดสถิติมีชิปไอคอนข้างตัวเลข** ตามต้นฉบับ แทนไอคอนจิ๋วหน้า label แบบเดิม

### ความต่างจาก `screen.png`

- **ไม่มีกราฟยอดขายรายเดือนในหน้านี้** ต้นฉบับมีกราฟแท่งเทียบปี 2023/2024
  โปรเจกต์มีกราฟอยู่แล้วที่ `/instructor/reports` (`MonthlyEarningsChart` + `SalesLineChart`)
  ซึ่งเป็นหน้าเฉพาะของรายงาน · การย้ายหรือทำซ้ำมาที่นี่จะได้กราฟสองที่ที่ต้องดูแลคู่กัน
- **ไม่มี "กิจกรรมล่าสุด"** ระบบยังไม่มี activity feed หรือ `AuditLog`
  (PLAN.md ระบุว่าเฟส 8 ยังไม่มี AuditLog) · ฟีดที่ไม่มีแหล่งข้อมูลคือกล่องเปล่าถาวร
- **ไม่มีการ์ด "คะแนนเฉลี่ย"** ระบบรีวิวยังไม่ได้ทำ (เฟส 7 ที่เหลือ)
- ตัวเลขในต้นฉบับเป็นค่าสมมติ (`฿452K`) · ของจริงดึงจาก API ทั้งหมด

### logic ที่รักษาไว้ครบ

`load()` ที่ยิง API พร้อมกัน, loading skeleton, error state + ปุ่มลองใหม่,
empty state เมื่อยังไม่มีคอร์ส, `CourseTable` พร้อม `CourseStatusBadge` และปุ่มจัดการ

---

## หน้า 10 — admin_dashboard → `/admin`

**สถานะ:** เสร็จ · typecheck ผ่าน · lint ผ่าน · build ผ่าน · screenshot ตรวจแล้ว

### สิ่งที่เปลี่ยน

- **`AdminNav` เปลี่ยนมาใช้ `SidebarLink` ตัวเดียวกับ instructor**
  ของเดิมมี active state อยู่แล้วและใช้เส้นทองเหมือนกัน แต่เป็น `border-l-2`
  ส่วนของ instructor ที่ผมเพิ่งทำเป็น `border-l-4` — **ต่างกัน 2 พิกเซลโดยไม่มีใครตั้งใจ**
  ตอนนี้ทั้งสอง sidebar ทำเครื่องหมาย "คุณอยู่ตรงนี้" ด้วยเส้นเดียวกันน้ำหนักเดียวกัน
- หัวข้อหน้าและหัวข้อสอง section ("งานที่รอคุณดำเนินการ" · "ตัวเลขใน 30 วันล่าสุด")
  ใช้ `SectionHeading` ทั้งหมด โดย section ที่มีปุ่มใช้ช่อง `action`

### แก้ปัญหาลำดับชั้นหัวข้อที่ screenshot จับได้

หลังเปลี่ยนหัวข้อทุกระดับมาใช้ `SectionHeading` **h1 กับ h2 ออกมาขนาดเท่ากันเป๊ะ**
หน้าจึงอ่านเป็นรายการของสิ่งที่เท่ากันหมด ไม่มีลำดับชั้น
แก้โดยให้ `SectionHeading` มี 3 ขนาด (`lg`/`md`/`sm`) ที่ปรับทั้งตัวอักษรและความสูงของแถบทองไปด้วยกัน
**ขนาดผูกกับระดับ heading เป็นค่าเริ่มต้น แต่ตั้งแยกได้** เพราะ section ของหน้า landing
เป็น `h2` ตามโครงสร้างเอกสาร แต่ต้องเป็นสิ่งที่ใหญ่ที่สุดบนจอ — การบังคับให้เป็น `h1`
เพื่อให้หน้าตาถูกจะทำให้มี `h1` หลายตัวในหน้าเดียว

### ความต่างจาก `screen.png`

- ต้นฉบับหน้านี้เป็นไฟล์เดียวใน 3 หน้า dashboard ที่ sidebar "Admin Portal" **ถูกต้องตามบริบท**
  (อีกสองหน้าคือ student/instructor ที่ถูก paste ผิด) แต่เมนูยังเป็นภาษาอังกฤษและเป็นเมนูสมมติ
  (Management/Analytics/Settings) ซึ่งไม่ตรงกับหลังบ้านจริง · ใช้เมนูจริง 6 รายการของโปรเจกต์
- ไม่มีกราฟในหน้านี้ · โปรเจกต์มีกราฟที่ `/admin/reports` แล้ว (ปุ่ม "ดูรายงานฉบับเต็ม" พาไป)
- ไม่มีเงา ตามข้อห้าม 18

### logic ที่รักษาไว้ครบ

การนับคิวงาน 3 ประเภท (สลิปรอตรวจ · คอร์สรออนุมัติ · บัญชีที่ถูกระงับ),
`allClear` เมื่อไม่มีงานค้าง, ตัวเลข 30 วันล่าสุดพร้อม `changePercent`
ที่แสดง "ไม่มีข้อมูลช่วงก่อนหน้าให้เทียบ" เมื่อเป็น `null` (ตาม CLAUDE.md หัวข้อ 8),
loading state, error `Alert`, การออกจากระบบใน sidebar

---

# สรุปภาพรวมทั้ง 10 หน้า

**typecheck ผ่าน · lint ผ่านทั้ง `src` · build ผ่านครบ 27 routes · screenshot ตรวจครบทุกหน้า**
แต่ละหน้า commit แยกกัน 8 commit

## component ใหม่ที่สร้างขึ้นระหว่างทาง

| component | หน้าที่ | เกิดที่หน้า | ถูกใช้ที่ไหนบ้าง |
|---|---|---|---|
| `shared/SectionHeading.tsx` | แถบทองคั่นหน้าหัวข้อ 3 ขนาด | 1 | ทุกหน้า |
| `ui/password-input.tsx` | ปุ่มแสดง/ซ่อนรหัสผ่าน | 2 | login · register · reset-password |
| `shared/BottomNav.tsx` | แถบล่างมือถือของผู้เรียน | 5 | 3 layout: `(student)` `(account)` `(public)` |
| `shared/SidebarLink.tsx` | เมนู sidebar ที่รู้หน้าปัจจุบัน | 9 | instructor + admin |
| `courses/CourseSortSelect.tsx` | เรียงลำดับเหนือผลลัพธ์ | 6 | `/courses` |

## component เดิมที่ถูกขยาย (กระทบทั้งระบบ)

- **`ui/card.tsx` — `CardTitle` มีแถบทองเป็นค่าเริ่มต้น** กระทบ `<CardTitle>` ทั้ง 14 จุดทั่วแอป
  รวมหน้าที่ไม่ได้อยู่ใน 10 หน้านี้ (จัดการคอร์ส · คิวสลิป · ห้องเรียน · โปรไฟล์ ฯลฯ)
- **`ui/input.tsx` — prop `icon`** ไอคอนนำหน้าในช่องกรอก
- **`ui/field.tsx` — prop `labelAction`** ช่องท้ายแถว label
- **`ui/progress.tsx` — prop `tone`** ทองสำหรับความคืบหน้าการเรียน
- **`shared/AuthFormHeader.tsx` — prop `icon` / `tone` / `align`**

## หน้าที่ screenshot ต่างจาก `screen.png` อย่างมีนัยสำคัญ

**ทุกหน้า** ต่างตรงที่**ไม่มี gradient และไม่มีเงา** (ข้อห้าม 18) การ์ดจึงดู flat กว่าต้นฉบับทั้งหมด
และ 6 หน้าที่ใช้ชุดสีเก่าจะ**ดูอ่อนกว่า**ต้นฉบับ เพราะยึด token `#1E3A5C` ไม่ใช่ `#022445`

ต่างเกินเรื่องสีและเงา เรียงตามความสำคัญ:

1. **student_dashboard** — ต้นฉบับมี **sidebar "Admin Portal" ในหน้าของผู้เรียน**
   และเนื้อหาเป็นภาษาอังกฤษเกือบทั้งหมด · ของจริงใช้ bottom nav + ภาษาไทย
   **ตัดการ์ด "Assignments" และ "Discussions" ออก** เพราะระบบไม่มีงานที่มอบหมาย
2. **instructor_dashboard** — ต้นฉบับมี sidebar "Admin Portal" เหมือนกัน (template ซ้ำ)
   **ตัดกราฟยอดขาย · กิจกรรมล่าสุด · คะแนนเฉลี่ย** ออก (มีที่ `/reports` แล้ว · ไม่มี AuditLog · ยังไม่มีรีวิว)
3. **login** — **ไม่มีปุ่ม "เข้าสู่ระบบด้วย Google"** (ไม่มี OAuth ในระบบ ข้อห้าม 12)
4. **course_detail** — **ไม่มีราคาเดิมขีดฆ่า** (ไม่มีแนวคิดส่วนลด) · เนื้อหาหลักสูตร
   เป็นรายการแบนไม่ใช่ accordion (schema ไม่มี `Section`)
5. **wallet_top_up** — **ไม่ใช่นาฬิกานับถอยหลัง** ใช้เวลาหมดอายุแทน
   (quote ไม่ได้จองอะไร · QR หมดอายุยังโอนเข้าได้)
6. **register** — ไม่มีช่องชื่อ/นามสกุลแยก (backend มี `displayName` ช่องเดียว)
   · การ์ดบทบาทกดแล้วไปต่อทันที ไม่มีปุ่ม "ดำเนินการต่อ"
7. **course_catalog** — ไม่มีป้าย "ขายดี" บนภาพปก (ไม่มีข้อมูลรองรับ)
   · ตัวกรองราคาเป็นช่องกรอกไม่ใช่ slider
8. **forgot_password / register** — ใช้เลย์เอาต์สองแผงร่วมกันทั้งตระกูล auth
   ไม่ใช่การ์ดลอยกลางจอ/navbar เต็มแบบที่ต้นฉบับแต่ละไฟล์วาดต่างกัน

## รายการ "ยังไม่ได้ทำ" ที่สะสมจากทุกหน้า

### ติดที่ข้อมูล/ฟีเจอร์ที่ระบบยังไม่มี (ต้องทำ backend ก่อน)
- ป้าย "ขายดี"/"ใหม่" บนการ์ดคอร์ส — ไม่มีข้อมูล
- ราคาเดิมขีดฆ่า / ส่วนลด / คูปอง — อยู่ใน "นอกขอบเขต" ของ PLAN.md
- accordion แบ่งบทในหน้าคอร์ส — schema ไม่มี `Section`
- "กิจกรรมล่าสุด" ในแดชบอร์ดผู้สอน — ไม่มี `AuditLog` (เฟส 8 ที่เหลือ)
- "คะแนนเฉลี่ย" — ยังไม่มีระบบรีวิว (เฟส 7 ที่เหลือ)
- การ์ด "งานที่มอบหมาย" ในหน้าแรกผู้เรียน — ไม่มีฟีเจอร์นี้ในระบบ

### ตัดสินใจไม่ทำโดยเจตนา
- ปุ่ม Google login — ข้อห้าม 12 และ CLAUDE.md หัวข้อ 2
- นาฬิกานับถอยหลัง QR — จะสัญญาเส้นตายที่ไม่มีจริง
- checkbox "ยอมรับข้อตกลง" ตอนสมัคร — ยังไม่มีหน้าข้อตกลงจริงให้ลิงก์ไป

### ข้อสังเกตที่ไม่เกี่ยวกับดีไซน์ แต่เจอระหว่างทาง
- **ภาพปกคอร์สทุกใบ 404 ทั้งระบบ** — `prisma/seed.ts` เขียน `coverKey` เป็น
  `cover/<key>/cover.jpg` แต่ไม่เคยอัปโหลดไฟล์จริงขึ้น MinIO
  `CourseCard` มี fallback `ImageOff` แต่ไม่ทำงาน เพราะ `coverUrl` ไม่ใช่ null
  (เซ็น URL สำเร็จ แต่ object ปลายทางไม่มี) · **เป็นปัญหาของ seed ที่มีมาก่อนงานนี้**
  ถ้าจะให้ demo ดูสมบูรณ์ ต้องอัปโหลดรูปตัวอย่างใน seed หรือให้ `signCover` ตรวจ `statObject` ก่อน

### ยังไม่ได้ตรวจ
- responsive 375px ตรวจแล้วเฉพาะ `/home`, `/courses`, `/wallet`, `/admin/users`, `/admin/content-reports`
  (อีก 7 หน้าในหัวข้อ "ปิดช่องว่าง" ด้านล่างตรวจแล้วเฉพาะ desktop 1440px)

---

## ปิดช่องว่าง "ยังไม่ได้ตรวจด้วยตา" — หัวข้อหน้านอก 10 หน้าหลัก

**บริบท:** รายการ "ยังไม่ได้ตรวจ" เดิมของเอกสารนี้ระบุว่าหน้านอก 10 หน้าหลัก (จัดการคอร์ส ·
ห้องเรียน · แบบทดสอบ · ถาม-ตอบ · คิวสลิป · จัดการผู้ใช้ · หมวดหมู่ · รายงาน · โปรไฟล์) ยังไม่มีใคร
เปิดดูจริงหลังการเปลี่ยนแปลง ทำให้ `UI_CONSISTENCY_AUDIT.md` (ตรวจแยกอีกรอบทีหลัง) เจอว่า
**หัวข้อหน้า (page title) ของ 12 ไฟล์ยังใช้ raw `<h1>` แทนที่จะเป็น `SectionHeading`** — ไม่ใช่บั๊ก
แต่เป็นเพราะไฟล์เหล่านี้ถูกสร้างในเฟส 2-8 เดิม**ก่อน**ที่ `SectionHeading` จะเกิดขึ้นตอนงานแปลงดีไซน์
(รวมถึง `ContentReportQueue.tsx` ที่สร้างทีหลังสุด เพราะคัดลอกแบบจาก `CourseReviewQueue.tsx`
ซึ่งมีปัญหาเดียวกันอยู่ก่อนแล้ว)

**แก้แล้วครบทั้ง 12 ไฟล์** แทนที่ `<header>...<h1 className="text-2xl font-semibold text-primary">
{title}</h1><p>...</p>...</header>` ด้วย `<SectionHeading as="h1" title={...} subtitle={...}
action={...} />` ตามแบบที่ `AdminDashboard.tsx`/`instructor/page.tsx`/`courses/page.tsx` ทำไว้ถูกต้อง
อยู่แล้ว — ปุ่ม/badge/filter ที่เคยอยู่ท้ายแถวหัวข้อทุกจุดย้ายเข้า prop `action` โดยตำแหน่งไม่เปลี่ยน:

| ไฟล์ | มี `action` |
|---|---|
| `(student)/my-courses/MyCoursesView.tsx` | ปุ่ม "ไปหน้ารวมคอร์ส" |
| `(student)/wallet/WalletView.tsx` | — |
| `(account)/profile/ProfileView.tsx` | — |
| `(student)/learn/[courseId]/qna/QnaBoard.tsx` | ปุ่ม "ตั้งคำถาม" (เฉพาะคนถามได้) |
| `(instructor)/instructor/reports/InstructorReportsView.tsx` | — |
| `(admin)/admin/courses/CourseReviewQueue.tsx` | badge จำนวนรออนุมัติ |
| `(admin)/admin/users/UsersTable.tsx` | — |
| `(admin)/admin/categories/CategoriesManager.tsx` | ปุ่ม "เพิ่มหมวดหมู่" |
| `(admin)/admin/reports/ReportsView.tsx` | แท็บช่วงเวลา 7/30/90 วัน |
| `(instructor)/instructor/qna/InstructorQnaInbox.tsx` | ฟอร์มค้นหา |
| `(admin)/admin/topups/TopupQueue.tsx` | badge จำนวนรอตรวจ |
| `(admin)/admin/content-reports/ContentReportQueue.tsx` | badge + ตัวกรองสถานะ |

**ไม่แตะ** `(instructor)/instructor/courses/[id]/page.tsx:153` เพราะเป็นชื่อคอร์สของผู้ใช้ ไม่ใช่
หัวข้อ section — คนละความหมายกับอีก 12 จุด

**ตรวจด้วย screenshot จริงจากเบราว์เซอร์ครบทั้ง 12 หน้า** (desktop 1440px ทุกหน้า + 375px มือถือ
3 หน้าตัวแทน: `/wallet`, `/admin/users`, `/admin/content-reports`) ยืนยันว่าแถบทองมาแล้วทุกจุด,
`action` ที่มีเนื้อหา (ปุ่ม/badge/แท็บ/ฟอร์ม) จัดวางถูกตำแหน่งเดิมและไม่ล้นบนจอเล็ก (ห่อบรรทัดใหม่ใต้
หัวข้อด้วย `flex-wrap` ของ `SectionHeading` เอง) · `QnaBoard.tsx` ตรวจด้วยโค้ด+typecheck+build เท่านั้น
เพราะต้องมีคอร์สที่ลงทะเบียนแล้วจริงถึงจะเข้าหน้านี้ได้ ไม่มีข้อมูล enrollment ใน seed ให้ทดสอบสด
แต่โครงสร้างเหมือนกับอีก 2 จุดที่ตรวจด้วย screenshot แล้ว (ปุ่มเดียวใน `action` แบบมีเงื่อนไข)

**เหตุการณ์ระหว่างทาง:** รัน `next build` (production) ในโฟลเดอร์เดียวกับ `next dev --turbopack`
ที่กำลังรันอยู่ ทำให้ `.next` เพี้ยนและทุกหน้าขึ้น "Internal Server Error" ชั่วคราว — ลบ `.next` แล้ว
ไม่พอ ต้อง restart ตัว process ของ dev server เอง (ได้รับความยินยอมจากผู้ใช้ก่อนทำ) — **บทเรียน:
ห้ามรัน `next build` ในโฟลเดอร์ frontend ขณะที่ dev server ตัวจริงกำลังรันอยู่คู่กัน** ใช้ typecheck/lint
และ screenshot จากเบราว์เซอร์จริงแทนเพื่อยืนยันความถูกต้องโดยไม่ต้องแตะ `.next` ของ dev server
