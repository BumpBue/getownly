# MIGRATION_LOG.md — แปลงดีไซน์ Google Stitch เป็น React component

บันทึกการแปลงดีไซน์จาก `design-reference/stitch_getownly_education_marketplace_ui/`
มาเป็นหน้าจริงในโปรเจกต์ · อ่านคู่กับ [CLAUDE.md](CLAUDE.md) และ [PLAN.md](PLAN.md)

---

## กติกาที่ใช้ร่วมกันทุกหน้า (ตกลงกับผู้ใช้แล้ว)

1. **สียึด token ของโปรเจกต์เป็นตัวตั้ง ไม่ใช่ค่าใน HTML ต้นฉบับ**
   ไฟล์ดีไซน์ 6 จาก 10 ไฟล์ใช้ชุดสีเก่า (`primary: #022445` · `secondary: #795823`)
   ส่วนอีก 4 ไฟล์ที่ลงท้าย `_updated_colors` ใช้ชุดใหม่ที่ถูกต้อง (`#1E3A5C` · `#C9A063`)
   ต้นตอคือ `prestige_thai_academy/DESIGN.md` ที่ยังไม่ได้อัปเดต
   **ทุกหน้าแปลงโดยใช้ `bg-primary` / `text-secondary` ของโปรเจกต์เสมอ**
   ผลคือ 6 หน้านั้นจะดูอ่อนกว่า `screen.png` เล็กน้อย ซึ่งเป็นความต่างที่ตั้งใจ
2. **ตัด gradient และเงาหนาทั้งหมด** (ข้อห้าม 18) ใช้ `border-border` + ระยะห่างแทน
3. **ไอคอนใช้ lucide-react** ซึ่งโปรเจกต์ใช้อยู่แล้ว ไม่เพิ่ม Tabler (ข้อห้าม 12)
   แมป Material Symbols → lucide ตามความหมายใกล้เคียงที่สุด
4. **เก็บ logic เดิมไว้ทั้งหมด** — fetch, state, error/loading/empty handling
   ห้ามลบฟังก์ชันที่ทำงานอยู่จริงเพียงเพราะดีไซน์ไม่ได้วาดไว้

---

## หน้า 1 — landing_page → `/`

**สถานะ:** เสร็จ · typecheck ผ่าน · lint ผ่าน · build ผ่าน · screenshot ตรวจแล้ว

### สิ่งที่เปลี่ยน

- **สร้าง `components/shared/SectionHeading.tsx`** — แถบทอง 4px คั่นหน้าหัวข้อ
  ซึ่งเป็นลายเซ็นของดีไซน์ชุดนี้ ทำเป็นคอมโพเนนต์เดียวเพราะจะถูกใช้ซ้ำทุกหน้า
  รับ `title` `subtitle` `action` (ปุ่มท้ายหัวข้อ) และ `as` สำหรับระดับ heading
- เปลี่ยนหัวข้อทั้ง 4 section ให้ใช้ `SectionHeading` — ของเดิมเป็นหัวข้อ**กึ่งกลาง**
  ตอนนี้เป็น**ชิดซ้าย + แถบทอง** ตามต้นฉบับ
- การ์ดหมวดหมู่เป็นสี่เหลี่ยมจัตุรัสตั้งแต่ `sm` ขึ้นไป ไอคอน `size-12` → `size-14`
  บนมือถือปล่อยความสูงอิสระ เพราะสี่เหลี่ยมจัตุรัส 4 ใบเรียงกันจะยาวเกินหนึ่งจอ
- `CourseCard` ได้ท้ายการ์ดแบบดีไซน์ — ราคามีเส้นทองคั่นซ้าย 4px
  พร้อมลูกศร `ArrowRight` ที่เลื่อนขวาตอน hover
  **คอร์สฟรีใช้เส้นสี `success` ไม่ใช่ทอง** เพราะแถบควรเห็นด้วยกับคำที่มันยืนข้างๆ
  ไม่ใช่ไปตั้งราคาให้ของที่ไม่มีราคา

### ความต่างจาก `screen.png`

- **ไม่มี gradient ใน hero และไม่มีเงาใต้การ์ด** — ต้นฉบับมี
  `bg-[radial-gradient(...)]`, `shadow-2xl`, `shadow-[0_4px_20px_rgba(30,58,92,0.08)]`
  ทั้งหมดชนข้อห้าม 18 การ์ดจึงดู flat กว่าภาพต้นฉบับ **นี่คือความต่างที่เห็นชัดที่สุด**
- ต้นฉบับใช้รูปถ่ายจาก `lh3.googleusercontent.com` ใน hero
  ผมเก็บ `HeroCourseMockup` ของเดิมไว้ เพราะเป็นคอมโพเนนต์ที่ทำงานได้จริงอยู่แล้ว
  ไม่ใช่ภาพประกอบที่ต้องหามาแทน

### สิ่งที่ยังไม่ได้ทำ

- **bottom nav bar บนมือถือ** — ยกไปทำเป็น layout ของกลุ่ม `(student)` ในหน้า student_dashboard
  เพื่อไม่ให้ได้ nav สองแบบที่ไม่ตรงกัน

### ข้อสังเกตที่ไม่เกี่ยวกับดีไซน์

- **ภาพปกคอร์สทุกใบ 404** — `prisma/seed.ts` เขียน `coverKey` เป็น
  `cover/<key>/cover.jpg` แต่**ไม่เคยอัปโหลดไฟล์จริงขึ้น MinIO**
  เป็นข้อจำกัดของ seed ที่มีมาก่อนงานนี้ ไม่ได้เกิดจากการแปลงดีไซน์
  `CourseCard` มี fallback `ImageOff` อยู่แล้วแต่ไม่ทำงาน เพราะ `coverUrl` ไม่ใช่ null
  (URL เซ็นได้สำเร็จ แต่ object ปลายทางไม่มีอยู่)
